version: v1
resources:
  - name: go-sample
    source: 
      name: web
    config:
      build:
        method: pack
      values:
        port: 8080
        container:
          env:
            normal:
              BPE_PORT: "8080"
